<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= book.title %> - Details</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <main class="book-details-container">
    <header>
      <h1><%= book.title %> <small>by <%= book.author %></small></h1>
    </header>

    <section class="book-info">
      <p><strong>📚 ISBN:</strong> <%= book.isbn || 'N/A' %></p>
      <p><strong>📌 Status:</strong>
        <% if (book.borrowed) { %>
          Borrowed by <%= book.borrowedBy %> on <%= new Date(book.borrowedDate).toLocaleDateString() %>
        <% } else { %>
          Available
        <% } %>
      </p>
    </section>

    <section class="book-actions">
      <% if (!book.borrowed) { %>
        <form action="/borrow/<%= book._id %>" method="POST" class="borrow-form">
          <button type="submit" class="btn borrow-btn">🔄 Borrow</button>
        </form>
      <% } %>
      <a href="/books/<%= book._id %>/edit" class="btn edit-btn">✏️ Edit</a>

      
    </section>

    <div class="back-link">
      <a href="/books">← Back to Books List</a>
    </div>
  </main>
</body>
</html><style>body {
  background: linear-gradient(135deg, #e3f2fd 0%, #fffde7 100%);
  font-family: 'Segoe UI', Arial, sans-serif;
}
.show-container, .view-container {
  background: #fff;
  max-width: 500px;
  margin: 3rem auto;
  padding: 2rem 2.5rem;
  border-radius: 12px;
  box-shadow: 0 6px 24px rgba(33,150,243,0.12);
}
h2 {
  color: #1976d2;
  margin-bottom: 1rem;
}
p, strong {
  font-size: 1.08rem;
  color: #444;
}
strong {
  color: #1976d2;
  font-weight: 600;
}
.status-available {
  color: #388e3c;
  font-weight: bold;
}
.status-borrowed {
  color: #d32f2f;
  font-weight: bold;
}

</style>
