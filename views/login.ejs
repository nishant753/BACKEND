<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login | UniShelf</title>
  <link href="https://fonts.googleapis.com/css?family=Pacifico&display=swap" rel="stylesheet">
  
</head>
<body>
  <header>
    <span class="header-title">UniShelf</span>
  </header>
  <main>
    <div class="login-container" role="main" aria-labelledby="loginTitle">
      <h1 id="loginTitle">Login</h1>
      <form action="/login" method="POST" aria-describedby="loginDesc" autocomplete="on">
        <div class="form-group">
          <label for="email">Email address</label>
          <input type="email" name="email" id="email" placeholder="Email" required autocomplete="username" aria-label="Email address">
        </div>
        <div class="form-group">
          <label for="password">Password</label>
          <div class="password-wrapper">
            <input type="password" name="password" id="password" placeholder="Password" required autocomplete="current-password" aria-label="Password">
            <button type="button" class="toggle-password" aria-label="Show password" tabindex="0" onclick="togglePassword()">
              <span id="eyeIcon">&#128065;</span>
            </button>
          </div>
        </div>
        <button type="submit" class="login-btn">Login</button>
      </form>
      <div class="login-links" id="loginDesc">
        <a href="/forgot">Forgot password?</a>
        <br>
        Don't have an account?
        <a href="/register">Register here</a>
      </div>
    </div>
  </main>
  <script>
    function togglePassword() {
      const pwd = document.getElementById('password');
      const icon = document.getElementById('eyeIcon');
      if (pwd.type === "password") {
        pwd.type = "text";
        icon.textContent = "🙈";
      } else {
        pwd.type = "password";
        icon.textContent = "👁️";
      }
    }
  </script>
</body>
</html>

  <style>
    body {
      font-family: 'Segoe UI', Arial, sans-serif;
      background-image: url("bookshelf.jpg");
      background-size: cover;
      background-repeat: no-repeat;
      background-attachment: fixed;
      margin: 0;
      min-height: 120vh;
      display: flex;
      flex-direction: column;
    }
    header {
      backdrop-filter: blur(5px);
      -webkit-backdrop-filter: blur(10px);
      color: #fff;
      padding: 1.2rem 2rem 1rem 2rem;
      box-shadow: 0 4px 18px rgba(0, 0, 0, 0.15);
      display: flex;
      align-items: center;
      justify-content: center;
      border-bottom-left-radius: 18px;
      border-bottom-right-radius: 18px;
      position: sticky;
      top: 0;
      z-index: 2;
    }
    .header-title {
      font-size: 2.5rem;
      font-weight: 600;
      font-family: 'Pacifico', cursive;
      letter-spacing: 2px;
      margin: 0;
      color: #fdfdfd;
      text-shadow: 1px 2px 4px rgba(0, 0, 0, 0.2);
      text-align: center;
      flex: 1;
    }
    .login-container {
      background: rgba(255,255,255,0.95);
      padding: 2.2rem 2rem 1.7rem 2rem;
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.20);
      width: 100%;
      max-width: 400px;
      margin: 3.5rem auto 0 auto;
      display: flex;
      flex-direction: column;
      align-items: stretch;
    }
    h1 {
      text-align: center;
      color: #23272b;
      margin-bottom: 1.3rem;
      font-size: 2rem;
      letter-spacing: 1px;
    }
    form {
      display: flex;
      flex-direction: column;
      gap: 1.1rem;
    }
    .form-group {
      display: flex;
      flex-direction: column;
      gap: 0.3rem;
    }
    label {
      font-size: 1rem;
      color: #23272b;
      font-weight: 500;
      margin-bottom: 0.2rem;
    }
    input[type="email"], input[type="password"] {
      padding: 0.75rem;
      border: 1.5px solid #bdbdbd;
      border-radius: 5px;
      font-size: 1rem;
      background: #f8fafc;
      transition: border-color 0.2s, box-shadow 0.2s;
    }
    input[type="email"]:focus, input[type="password"]:focus {
      border-color: #67d6bc;
      box-shadow: 0 0 0 2px #67d6bc33;
      outline: none;
      background: #fff;
    }
    .password-wrapper {
      position: relative;
      display: flex;
      align-items: center;
    }
    .toggle-password {
      position: absolute;
      right: 12px;
      background: none;
      border: none;
      color: #888;
      font-size: 1.1rem;
      cursor: pointer;
      padding: 0;
      transition: color 0.2s;
    }
    .toggle-password:hover {
      color: #67d6bc;
    }
    .login-btn {
      background-color: #67d6bc;
      color: #fff;
      padding: 0.8rem;
      border: none;
      border-radius: 5px;
      font-size: 1.08rem;
      font-weight: 600;
      cursor: pointer;
      transition: background 0.2s, transform 0.15s;
      margin-top: 0.3rem;
      box-shadow: 0 2px 8px rgba(25, 118, 210, 0.10);
    }
    .login-btn:hover {
      background: linear-gradient(90deg, #67d6bc 0%, #5ea997 100%);
      transform: translateY(-2px) scale(1.04);
    }
    .login-links {
      text-align: center;
      margin-top: 1.2rem;
      font-size: 0.96rem;
      color: #555;
    }
    .login-links a {
      color: #67d6bc;
      text-decoration: none;
      font-weight: bold;
      margin-left: 0.2rem;
    }
    .login-links a:hover {
      text-decoration: underline;
    }
    @media (max-width: 600px) {
      .header-title { font-size: 1.5rem; }
      .login-container { padding: 1.2rem 0.7rem; margin-top: 1.3rem; }
      h1 { font-size: 1.3rem; }
    }
  </style>